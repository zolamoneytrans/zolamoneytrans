{
  "name": "zola-money-trans-functions",
  "description": "Zola Money Trans — Firebase Cloud Functions (FreshPay proxy)",
  "scripts": {
    "lint": "eslint --ext .js .",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "cors": "^2.8.5",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.9.0",
    "node-fetch": "^2.7.0",
    "nodemailer": "^8.0.9"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-google": "^0.14.0"
  },
  "private": true
}
